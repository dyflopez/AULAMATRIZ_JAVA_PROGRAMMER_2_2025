package collection;

public class CollectionMain {

    /**
     * collection:  es una interfaz de java que permite almacenar elementos en una lista, conjunto o mapa.
     * List, set,Map, Queue
     * cuando se usa Collection estamos heradonda de Iterable y  continen los siguientes metodos:
     *   - boolean add(E e): agrega un elemento a la colección.
     *   - Contains(Object o): verifica si un elemento está presente en la colección.
     *   - boolean remove(Object o): elimina un elemento de la colección.
     *   - int size(): devuelve el número de elementos en la colección.
     *   - boolean isEmpty(): verifica si la colección está vacía.
     *   - Stream<E> stream(): devuelve un flujo de elementos de la colección.
     *
     *   Stream s = lista
     *   operan  s
     *   utulizar de nuevo s .... falla.... contenedor que vacia despues de una operacion
     */


}
